[package]
name = "ffi_utils"
version = "0.1.0"
edition = "2021"

[lib]
name = "ffi_utils"
path = "src/lib.rs"

[dependencies]
parking_lot = "0.12"
once_cell = "1.20"
regex = "1.11"

[features]
default = []
